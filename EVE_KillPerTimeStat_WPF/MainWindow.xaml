<Window x:Class="EVE_KillPerTimeStat_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EVE_KillPerTimeStat_WPF"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="415.804" Width="751.975">
    <Window.Resources>
        <DataTemplate x:Key="KillTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding KillerName}"></TextBlock>
                <TextBlock Text="{Binding SystemName}"></TextBlock>
                <TextBlock Text="{Binding KillerName}"></TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid/>
</Window>
